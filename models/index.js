const user = require('./user'),
  order = require('./order'),
  category = require('./category'),
  item = require('./item'),
  cafe = require('./cafe'),
  client = require('./client');

module.exports = {
  User: user,
  Order: order,
  Category: category,
  Item: item,
  Cafe: cafe,
  Client: client,
}
